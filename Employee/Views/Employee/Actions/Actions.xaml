<UserControl x:Class="Employee.Views.Employee.Actions.Actions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top section with buttons -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="10">
            <Button Content="Add Employee" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddEmployeeCommand}"/>
            <Button Content="Edit Employee" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Delete Employee" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Search Employees" Style="{StaticResource PrimaryButtonStyle}"/>
            <Button Content="Export CSV" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ExportToCsvCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
